// URL do backend quando o frontend e servido de OUTRO lugar (ex.: Netlify).
// Deixe vazio ('') quando o proprio Express serve esta pagina (local ou Render
// de origem unica) -- tudo funciona em mesma origem, sem CORS.
// Exemplo separado: window.WEB_CHAT_BACKEND_URL = "https://meu-chat.onrender.com";
window.WEB_CHAT_BACKEND_URL = "";
